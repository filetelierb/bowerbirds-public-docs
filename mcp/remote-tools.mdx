---
title: "Tools"
description: "The tools the remote MCP door offers, and the two ideas behind them: payload URLs and signatures."
icon: "toolbox"
---

A Bucket token acts on its Bucket only. A workspace token names the Bucket on every call with `slug` (and `owner` when it is not the workspace itself).

| Tool | Purpose |
| --- | --- |
| `list_items` | Fetch records newest first with filters, in pages |
| `get_capture` | Read one record whole: content, parsed comments, payload manifest, `signed` |
| `list_captures` | The older text-only sweep (`limit`, `since`) |
| `list_directories` | The Bucket's directory paths, for filing and moving |
| `add_capture` | File a record and receive one upload URL per declared payload |
| `move_items` | Move records to a directory, in this Bucket or another one of the workspace |
| `trash_items` | Move records to the Trash; a person can restore them |
| `sign_items` / `unsign_items` | Put this token's mark on records, or take it off |
| `add_finding` | A short note at the Bucket root |
| `list_routes`, `claim_cycle`, `renew_cycle`, `release_cycle`, `derive_record`, `sign_items` with `items` | The routing tools, advertised to a token with the `router` role; see below |
| `get_record_context` | Read the findings the media processor condensed a record's video, audio, PDF or images into — a summary, then detailed descriptions each citing the chunk and moment it came from — or one `part` (`summary`, `findings`, `chunks`); free, any role. See [Media findings](/cloud/media#mcp-tools) |
| `get_media_chunk` | What the processor observed in ONE chunk as it read it, behind a finding's description; `router` role (and `admin`), free |
| `search_records` | One Bucket's records in words — titles, text and a capture's comments — to find what already exists before filing something new; `router` role (and `admin`), free |
| `get_record` | Read one record whole by id: content, comments, tags, payload manifest; `router` role (and `admin`), free |
| `process_media` | Compute a record's findings now through the platform's processor; `router` role only, charges the workspace's credits |

## `list_items`

```json
{
  "slug": "qa",
  "kind": ["capture", "note"],
  "path": "login",
  "since": "2026-09-01T00:00:00Z",
  "signed": false,
  "limit": 50
}
```

- `kind` keeps one or several record kinds.
- `path` scopes to a directory's subtree; `exact: true` narrows it to that directory alone.
- `since` keeps records changed strictly after an RFC3339 instant.
- `signed` keeps records this token has signed (`true`) or has not (`false`).
- Pages hold at most 200 records; pass the answer's `nextCursor` as `cursor` for the next page.

Every record answers `signed: true|false` for the calling token.

## `get_capture`

```json
{ "slug": "qa", "item_id": "<record-id>" }
```

A capture's `content` is the numbered list of comments about the picture; `comments` is the same list parsed out. The payload manifest lists `{name, role, mimeType, size, url}` for each file. By convention a capture carries `annotated.png` (role `annotated`, the picture with the pins drawn on it), `raw.png` (role `original`) and `session.json` (role `capture-session`, the editable annotations).

## Payloads

Payload bytes never cross the MCP door. Each `url` is **short-lived (15 minutes) and self-authenticating**: fetch it with a plain HTTP GET and no header. A URL stops working the moment its token is revoked.

`add_capture` works the same way for uploads:

```json
{
  "slug": "qa",
  "title": "Login form",
  "content": "Global comments:\n 1. Button off-center",
  "directoryPath": "login",
  "tags": ["ci"],
  "payloads": [
    { "name": "annotated.png", "mimeType": "image/png", "size": 183211, "role": "annotated" },
    { "name": "raw.png", "mimeType": "image/png", "size": 160004, "role": "original" }
  ]
}
```

It files the record and answers one upload URL per declared payload. PUT the bytes to it with exactly the `Content-Type` and `Content-Length` it lists, within 15 minutes. Bytes that differ from what was declared are refused before anything is stored. `kind` defaults to `capture`; `note` and `file` are accepted.

## Signatures

A signature is the token's own mark on a record. It changes nothing on the record, so any role that can read may sign. Use it as the agent's queue: `list_items` with `signed: false` is what this token has not handled yet; `sign_items` after handling a record keeps it out of the next sweep.

Another token sees only its own marks. A person's session sees no `signed` field and cannot sign. Revoking a token removes its marks.

## `move_items` and `trash_items`

```json
{ "slug": "qa", "ids": ["<id>", "<id>"], "toBucketSlug": "archive", "toDirectoryPath": "done" }
```

Moves follow the app's rules: write access on both sides, a record in the Trash refuses, a deleted or intake Bucket refuses, and a move to where the record already is succeeds and changes nothing. `trash_items` takes `{ "slug", "ids" }` and answers what was trashed, what was already gone, and what refused. Nothing here deletes for good.

## Routing tools

A token with the `router` role is an [external router](/cloud/external-routers)'s brain: the router supervising a Bucket names it, and the door advertises five more tools to it. A Bucket is the address; the **router** is what a cycle claims and what signs, and it may supervise several Buckets. They write the same route marks and router status a native router writes, and every write is judged by the same gate first.

| Tool | Purpose |
| --- | --- |
| `list_routes` | The router supervising this Bucket: every Bucket it supervises with the folders it watches in each and whether this token can read it, its global `kinds`/`keywords`/`ageDays` filters, and its routes — destinations, connectors with their tool grants, description, goal, rules as prose, and whether the route may create external items unattended |
| `claim_cycle` | Take the **router's** cycle marker under a lease (10 minutes by default, at most 60) and receive the `cycleId` the marks carry. One cycle covers every Bucket the router supervises |
| `renew_cycle` / `release_cycle` | Extend the lease; let it go when the run is done |
| `derive_record` | A new record from a source — `split` or `augment` — with provenance and the source's payload bytes copied, inside the route's destinations. With `mergeInto` or `related` it lands in a Package beside what it relates to. The source is never written |
| `sign_items` with `items` | The source's route marks and router mark: a `status` (`pending`, `kept`, `routed`, `processed`) and one link per route (`derive`, `merge`, `create`, `append`, `link`, `none`) with the destination item's id and, optionally, the finding ids it rested on. `slug` names the Bucket those records are in, so a brain that read several signs once per Bucket |

`sign_items` keeps its plain `ids` shape for every role: a token's own mark, unchanged. A `router` token calling `move_items` or `trash_items` is refused with `router_role`; routers only add.

Every record a router handled answers `routerStatus {status, cycleId, at, missing}`, `links [{route, kind, destination}]` and, on a derived record, `derivedFrom` — from `list_items`, `get_capture` and the CLI alike. A record the media processor has read also carries a `context` stamp (`version`, `at`, `fingerprint`, `tokens`, and the counts `findings` and `chunks`) on `get_capture` and `list_captures`; the whole document is `get_record_context`'s, see [Media findings](/cloud/media#the-findings-document).

## What an agent cannot do

Mint credentials wider than itself, read the Trash, read another workspace, switch workspaces, or reach a Bucket that takes submissions through a publishable web key only. Every refusal names its reason.
